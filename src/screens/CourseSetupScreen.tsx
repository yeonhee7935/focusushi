import { View, Text } from "react-native";
export default function CourseSetupScreen() {
  return (
    <View
      style={{ flex: 1, alignItems: "center", justifyContent: "center", backgroundColor: "#0006" }}
    >
      <Text style={{ color: "#fff" }}>CourseSetup</Text>
    </View>
  );
}
